# Intentionally minimal -- all customisations stripped while we debug
# whether tmux defaults work for copy/paste to the Mac clipboard.
# If the bare-default tmux works, our previous customisations were the
# cause and we'll re-add them one at a time. If it doesn't, the problem
# is elsewhere (tmux build, terminal, etc.) and we keep digging.
